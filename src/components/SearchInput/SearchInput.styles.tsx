import { createStyles } from '@material-ui/core';
const styles = () =>
  createStyles({
    root: {
      width: '50%',
      minWidth: '300px',
      maxWidth: '1200px',
    },
  });

export default styles;
